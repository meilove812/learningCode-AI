# 一、二年级语文写字页面统一规范

本规范适用于一年级和二年级所有生字卡、写字卡及课程中的写字模块。新增或改造页面必须逐字同时提供以下五项，不能只显示静态字形或笔画名称：

1. **真实笔顺动画**：使用仓库内 Hanzi Writer 与本地 `hanzi-data/*.json`，可以逐画播放、暂停/继续、重播；不得调用远程字形服务。
2. **描红与跟写**：提供按实际笔顺逐画描红的交互区，并提供可打印的淡色描红字模和至少两个空白田字格。
3. **笔顺文字描述**：按实际顺序列出每一画的规范名称；文字项数量必须与本地 JSON 的 `strokes` 数量一致，并抽样核对名称与字形轨迹一致。
4. **字音词语**：同时显示汉字、带调拼音和重新编写或选取的语境词语。
5. **有声学习**：点击单字或整卡可以朗读“汉字 + 读音 + 词语”，同一区域提供连续播放。

## 实现与验收

- 默认复用 `grade2-up/chinese/unit1/assets/hanzi-writer.min.js`；字形数据按页面相对路径从仓库加载。
- 每个字的数据应有唯一 `data-hanzi`，并标注 `data-strokes="笔画名、…"`；自动检查负责比对卡片、JSON 文件和笔画数量。
- 动画控制必须包含播放、暂停/继续和重播；描红控制必须包含重来。打印样式必须显示淡色字模、田字格中线及空白跟写格，并隐藏按钮、弹窗和导航。
- 语音优先 Microsoft Xiaoxiao Online (Natural)，监听 `voiceschanged`，提供语音、语速、单项与连续播放。
- 发布前运行 `python3 scripts/check-writing-pages.py`；再抽样打开每个年级至少 3 个字，逐项验证播放暂停/重播、每一画描红、打印预览、笔画文字顺序及朗读。
